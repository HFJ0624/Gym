<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sau.gym.admin.mapper.McpDocImportMapper">

    <!-- 插入 MCP 文档导入记录 -->
    <insert id="insertImportRecord">
        INSERT INTO rag_mcp_doc_import (
            file_name,
            relative_path,
            title,
            category,
            section_count,
            chunk_count,
            status,
            error_message,
            create_by,
            create_time
        )
        VALUES (
                   #{fileName},
                   #{relativePath},
                   #{title},
                   #{category},
                   #{sectionCount},
                   #{chunkCount},
                   #{status},
                   #{errorMessage},
                   #{createBy},
                   NOW()
               )
    </insert>

</mapper>