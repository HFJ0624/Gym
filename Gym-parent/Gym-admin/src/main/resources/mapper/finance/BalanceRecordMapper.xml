<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sau.gym.admin.mapper.BalanceRecordMapper">

    <!--  插入流水信息   -->
    <insert id="insertOne">
        insert into balance_record (
            user_id,
            order_no,
            amount,
            before_balance,
            after_balance,
            type,
            remark,
            create_time
        ) values (
                 #{userId},
                 #{orderNo},
                 #{amount},
                 #{beforeBalance},
                 #{afterBalance},
                 #{type},
                 #{remark},
                 #{createTime}
                 )
    </insert>

</mapper>