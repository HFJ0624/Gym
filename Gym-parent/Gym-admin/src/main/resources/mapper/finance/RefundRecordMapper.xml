<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sau.gym.admin.mapper.RefundRecordMapper">

    <!--  插入退款流水信息   -->
    <insert id="insertOne">
        insert into refund_record (
            refund_no,
            pay_no,
            order_no,
            user_id,
            order_type,
            refund_amount,
            status,
            refund_time,
            create_time
        ) values (
                     #{refundNo},
                     #{payNo},
                     #{orderNo},
                     #{userId},
                     #{orderType},
                     #{refundAmount},
                     #{status},
                     #{refundTime},
                     #{createTime}
                 )
    </insert>

</mapper>