<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sau.gym.admin.mapper.MenuMapper">

    <resultMap id="MenuMap" type="com.sau.gym.model.entity.menu.Menu" autoMapping="true"></resultMap>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,parent_id,title,component,sort_value,status,create_time,update_time,is_deleted
    </sql>

    <!--  找到所有节点  -->
    <select id="selectAll" resultMap="MenuMap">
        select <include refid="columns" />
        from menu
        where
        is_deleted = 0
        order by sort_value
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into menu (
            id,
            parent_id,
            title,
            component,
            sort_value,
            status
        ) values (
                     #{id},
                     #{parentId},
                     #{title},
                     #{component},
                     #{sortValue},
                     #{status}
                 )
    </insert>

    <update id="updateById" >
        update menu set
        <if test="parentId != null and parentId != ''">
            parent_id = #{parentId},
        </if>
        <if test="title != null and title != ''">
            title = #{title},
        </if>
        <if test="component != null and component != ''">
            component = #{component},
        </if>
        <if test="sortValue != null">
            sort_value = #{sortValue},
        </if>
        <if test="status != null">
            status = #{status},
        </if>
        update_time =  now()
        where
        id = #{id}
    </update>

    <select id="countByParentId" resultType="Integer">
        select count(id)
        from menu
        where
            parent_id = #{parentId}
          and is_deleted = 0
    </select>

    <update id="deleteById">
        update menu set
            update_time = now() ,
            is_deleted = 1
        where
            id = #{id}
    </update>

    <select id="selectListByUserId" resultMap="MenuMap">
        SELECT DISTINCT m.* FROM menu m
                                     INNER JOIN role_menu rm ON rm.menu_id = m.id
                                     INNER JOIN user_role ur ON ur.role_id = rm.role_id
        WHERE ur.user_id=#{userId} and m.is_deleted = 0
    </select>

    <select id="selectById" resultMap="MenuMap">
        select <include refid="columns" /> from menu where id = #{parentId}
    </select>

</mapper>