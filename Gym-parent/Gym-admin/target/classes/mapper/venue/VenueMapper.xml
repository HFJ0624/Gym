<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sau.gym.admin.mapper.VenueMapper">


    <!--  映射查询到的字段 -->
    <resultMap id="venueMap" type="com.sau.gym.model.entity.venue.Venue" autoMapping="true"></resultMap>


    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,venue_name,venue_type,location,phone,capacity,description,status,open_time,close_time,create_time,update_time,status,is_deleted,avatar
    </sql>

    <sql id="findPageWhere">
        <where>
            <if test="venueName != null and venueName != ''">
                and venue_name like CONCAT('%',#{venueName},'%')
            </if>
            <if test="venueType != null and venueType != ''">
                and venue_type like CONCAT('%',#{venueType},'%')
            </if>
            <if test="phone != null and phone != ''">
                and phone like CONCAT('%',#{phone},'%')
            </if>
            and is_deleted = 0
        </where>
    </sql>

    <!--  角色列表查询方法  -->
    <select id="findByPage" resultMap="venueMap">
        select <include refid="columns" />
        from venue
        <include refid="findPageWhere"/>
        order by id
    </select>

    <!-- 新增角色 -->
    <insert id="saveVenue">
        insert into venue (
            id,
            venue_name,
            venue_type,
            location,
            phone,
            capacity,
            description,
            open_time,
            close_time,
            avatar
        ) values (
                     #{id},
                     #{venueName},
                     #{venueType},
                     #{location},
                     #{phone},
                     #{capacity},
                     #{description},
                     #{openTime},
                     #{closeTime},
                     #{avatar}
                 )
    </insert>

    <!-- 修改场馆 -->
    <update id="updateVenue">
        update venue set
        <if test="venueName != null and venueName != ''">
            venue_name = #{venueName},
        </if>
        <if test="venueType != null and venueType != ''">
            venue_type = #{venueType},
        </if>
        <if test="location != null and location != ''">
            location = #{location},
        </if>
        <if test="phone != null and phone != ''">
            phone = #{phone},
        </if>
        <if test="capacity != null and capacity != ''">
            capacity = #{capacity},
        </if>
        <if test="description != null and description != ''">
            description = #{description},
        </if>
        <if test="openTime != null">
            open_time = #{openTime},
        </if>
        <if test="closeTime != null">
            close_time = #{closeTime},
        </if>
        <if test="avatar != null and avatar != ''">
            avatar = #{avatar},
        </if>
        update_time =  now()
        where
        id = #{id}
    </update>

    <!-- 根据id删除场馆信息 -->
    <update id="deleteById">
        update venue set is_deleted = 1 where id = #{venueId}
    </update>

    <!-- 查询所有的角色数据 -->
    <select id="findAllVenue" resultMap="venueMap">
        select id,venue_name,location
        from venue where is_deleted = 0
    </select>

    <select id="selectOne" resultType="com.sau.gym.model.entity.venue.Venue">
        select <include refid="columns" /> from venue where venue_name = #{venueName} and is_deleted = 0
    </select>

    <sql id="get">
        <where>
            <if test="venueName != null and venueName != ''">
                and venue_name like CONCAT('%',#{venueName},'%')
            </if>
            <if test="venueType != null and venueType != ''">
                and venue_type like CONCAT('%',#{venueType},'%')
            </if>
            and is_deleted = 0 and status = 1
        </where>
    </sql>

    <!-- 查询所有的角色数据 -->
    <select id="getAllVenue" resultMap="venueMap">
        select <include refid="columns" />
        from venue
        <include refid="get"/>
    </select>

    <select id="selectByUserId" resultMap="venueMap">
        select v.id,v.venue_name,v.venue_type,v.location,v.phone,v.capacity,v.description,v.status,
               v.open_time,v.close_time,v.create_time,v.update_time,v.status,v.avatar
        from venue v
        left join venue_collect vc on vc.venue_id = v.id
        where vc.user_id = #{id}
        and v.is_deleted = 0
    </select>

    <update id="updateVenueStatus">
        UPDATE venue SET
            status = #{status}
        WHERE id = #{id}
          and is_deleted = 0
    </update>

    <!-- 获取好评率前6的场馆ID集合 -->
    <select id="getVenueByRate" resultType="java.lang.Long">
        SELECT v.id
        FROM venue v
                 LEFT JOIN venue_comment c
                           ON v.id = c.venue_id
                               AND c.status = 1
                               AND c.is_deleted = 0
        GROUP BY v.id
        ORDER BY
        -- 按好评率降序排序（和之前的计算逻辑完全一致）
        ROUND(
                CASE WHEN COUNT(c.id) = 0 THEN 0
                     ELSE (SUM(CASE WHEN c.esteem > 3 THEN 1 ELSE 0 END) / COUNT(c.id)) * 100
                    END,
                2
        ) DESC,
            v.id ASC
        LIMIT 6
    </select>

    <!-- 2. 根据ID集合批量查询场馆信息 -->
    <select id="selectVenueBatchByIds" resultMap="venueMap">
        SELECT * FROM venue
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        <!-- 👇 加这一行，强制按传入的ID顺序排序 -->
        ORDER BY FIELD(id,
        <foreach collection="ids" item="id" separator=",">
            #{id}
        </foreach>
        )
    </select>

</mapper>