<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sau.gym.admin.mapper.VenueVisitMapper">

    <update id="addVisit">
        UPDATE venue_visit SET nums = nums + 1 WHERE venue_id = #{venueId};
    </update>

    <insert id="addOne">
        insert into venue_visit
        (venue_id)
        values(#{id})
    </insert>

    <select id="selectALL" resultType="com.sau.gym.model.vo.venue.VenueVisitVO">
        select vv.id,v.venue_name as venueName,vv.nums,vv.create_time
        from venue_visit vv
        left join venue v on v.id = vv.venue_id
        order by vv.nums desc
    </select>

</mapper>