<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sau.gym.admin.mapper.RoleUserMapper">

    <delete id="deleteByUserId">
        delete from user_role sur where sur.user_id = #{userId}
    </delete>

    <insert id="doAssign">
        insert into user_role(user_id , role_id , create_time , update_time , is_deleted)
        values ( #{userId} , #{roleId} , now() , now() , 0)
    </insert>

    <!-- 查询指定用户所对应的角色数据 -->
    <select id="findUserRoleByUserId" resultType="long">
        select role_id from user_role sur where sur.user_id = #{userId}
    </select>

</mapper>