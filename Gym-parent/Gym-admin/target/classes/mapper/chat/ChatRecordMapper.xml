<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sau.gym.admin.mapper.ChatRecordMapper">

    <!-- 插入聊天记录 -->
    <insert id="insertChatRecord">
        INSERT INTO chat_record(user_id, username,session_id, user_message, ai_reply, create_time)
        VALUES(#{userId},#{username}, #{sessionId}, #{userMessage}, #{aiReply}, #{createTime})
    </insert>

    <!-- 查询用户最近对话（记忆上下文） -->
    <select id="selectUserRecentHistory" resultType="com.sau.gym.model.entity.chat.ChatRecord">
        SELECT user_message, ai_reply
        FROM chat_record
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{limit}
    </select>

</mapper>