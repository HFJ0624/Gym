<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sau.gym.admin.mapper.ChatConversationMapper">

    <resultMap id="BaseResultMap" type="com.sau.gym.model.entity.chat.ChatConversation">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="user_avatar" property="userAvatar"/>
        <result column="admin_id" property="adminId"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectByUserId" resultMap="BaseResultMap">
        select * from chat_conversations
        where user_id = #{userId} limit 1
    </select>

    <insert id="insert" parameterType="com.sau.gym.model.entity.chat.ChatConversation" useGeneratedKeys="true" keyProperty="id">
        insert into chat_conversations
        (user_id, user_name, user_avatar, admin_id, status, created_at, updated_at)
        values
            (#{userId}, #{userName}, #{userAvatar}, #{adminId}, #{status}, now(), now())
    </insert>

    <select id="selectAllUserList" resultMap="BaseResultMap">
        select * from chat_conversations
        order by updated_at desc
    </select>

</mapper>